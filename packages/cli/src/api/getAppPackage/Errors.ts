enum Errors {
    /**
     * We failed to read the app package.
     */
    FAILED_TO_READ_PACKAGE = 'FAILED_TO_READ_PACKAGE',

    /**
     * The package file has invalid JSON
     */
    INVALID_PACKAGE_JSON_CONTENT = 'INVALID_PACKAGE_JSON_CONTENT'
}

export default Errors;
